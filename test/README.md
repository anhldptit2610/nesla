this folder contains programs to test the behavior of nesla.

Note:
    1. CPU test programs(cpu_test*) assume you have the TomHarte's ProcessorTests
       in this folder. You can find that test on github.